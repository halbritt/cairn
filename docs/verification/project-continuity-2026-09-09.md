# Retaining project direction — 2026-09-09

The owner's recent corrections changed what Cairn should optimize for: sustained
task value, including qualitative benefits; two existing harnesses before more
adapters; and complete implementation history rather than a replacement summary.
Those decisions were in the conversation and repository documents, but were not
represented in the twelve eligible note previews returned by two ordinary browse
pages. A search for task-value evaluation priorities returned implementation notes.
This was a bounded inspection of the hosted profile, not proof about inaccessible
records or every phrase in the existing note bodies.

Three concise, source-linked ordinary notes now retain the priorities, evaluation
approach and history preference. They cite the current roadmap/status at `fff9b66`
and direct consumers to verify current source. They remain agent-authored summaries
of the recorded owner direction, not new authority or an import of raw conversation.
Fresh task/run retrieval found and pulled each exact note. The priorities question
missed in lexical search and found the note first with the existing semantic route;
the other two questions found their notes first lexically. The ranker was unchanged.

Repository agent guidance now includes decisions and preferences in task-start
retrieval and calls for selective maintenance after material owner corrections.
Existing-note review, source pointers, privacy boundaries and current-instruction
precedence remain explicit. This addresses a capture gap in the actual workflow;
it does not add an automatic extraction or grooming service.

The expected value is continuity: later work can recover why priorities changed
and avoid asking the owner to repeat corrections. That value is not yet established
by these checks. This session already had the conversation and repository documents,
so its decisions cannot be attributed uniquely to the saved notes. Subsequent work
can provide qualitative evidence about whether the notes help and what they cost.
Do not manufacture a control, declare a prevented mistake, or substitute successful
retrieval for that observation.

Inspection of `core/assessment.go` and the [use/outcome contract](../use-outcome-loop.md)
also found that assessments already retain narrative reasons, evidence references,
method, observer and revision history. Unknown task acceptance can coexist with a
qualified narrative observation. No new assessment schema or numerical value score
was added. A run assessment is still tied to a receipt; broader longitudinal case
analysis may span records and artifacts outside that one-run structure.

[Metadata](project-continuity-2026-09-09.json) identifies the retained versions,
source hashes and private artifact pointers. No code, server configuration or
installed binary changed. Documentation links and preservation of the earlier
implementation-status narrative were checked; no code tests were needed.
