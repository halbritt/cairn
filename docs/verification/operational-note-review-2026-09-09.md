# Operational note review, 2026-09-09

The ordinary hosted profile retrieved nine eligible repository notes across two
browse pages. A bounded review of their current setup, scope, discovery and
permission instructions found one ambiguous permission example and two procedures
that needed the newly installed discovery options. Seven notes were left
unchanged. This was selected maintenance by the coding agent, not an automatic
groomer or comprehensive requalification of historical experiment claims.

The OpenCode procedure presented a wildcard-deny permission example without its
restriction to a standalone memory-only task. The current
[MCP guide](../mcp.md#opencode-example) makes that condition explicit. The saved
procedure now does too, distinguishes MCP names from custom-tool names, and
explains that the default configuration fragment leaves host permissions to the
host. Actual installed `opencode-config` output was checked in default and
`--memory-only` modes. No host permission file was changed.

The OpenCode and Codex procedures also now explain optional semantic discovery,
labelled fallback, source verification and its latency cost. The Codex procedure
adds the existing browse-continuation instructions. Both edits used body-only
revision with the version obtained from a fresh authenticated pull; metadata and
record identities were preserved. Their versions advanced from 6 to 7 and from
4 to 5 respectively.

A fresh native Codex client retrieved its procedure at rank 1 and pulled the
exact revised body. A normal OpenCode session with scripted loopback completions
asked how to add memory tools without disabling tools needed to edit files. It
retrieved the revised OpenCode procedure first and pulled the exact body. Both
used the enabled semantic worker and actual conversation/session scopes.
Codex started no model turn and OpenCode used no model inference. These checks
establish that the maintained guidance is reachable through the installed tools;
they do not establish autonomous correction or improved model task outcomes.

[Metadata](operational-note-review-2026-09-09.json) retains the versions, hashes,
preserved fields and local evidence pointers. Operational note bodies, requests
and tool output remain outside Git. Source was inspected at `87356a8`; the
installed binary remains the clean `ecbcc54` build. No code, service configuration
or deployment changed during this review.
