# Observed compact indexes — 2026-09-09

Fresh and retained indexes now pass through the existing process observer while
ordinary native tools expand their selected records. Migration 031 permits one
reader designation per index. The observer keeps receipt ownership; destination,
currentness, expiry and shared expansion budgets still apply.

The [contract and commands](../observed-index.md) describe the new route and its
limits. Body execution remains the default. The preserved semantic context file
and the digest of actual delivered input serve different purposes.

## Verification

- Full `make test-integration` passed on disposable PostgreSQL, including Go race
  coverage and actual CLI/stdio MCP workflows. `make check` and 40 Python tests
  passed. Tests cover ordinary-role and repository/destination restrictions,
  exact retries, stale sources, shared body/evidence credits, expiry, retained
  handles/budget, and owner-only inspection and launch.
- The real CLI child checks execute fresh and retained indexes through stdin and
  argv. Children pull a note and its exact cited source with ordinary credentials.
  Receipt/outcome inspection remains with the observer. Mismatched reader,
  context, query, budget, index mode and tool declarations refuse before binding.
  Retained semantic, browse and ranked-page declarations must match; identical
  receipts do not authorize another launch.
- Native OpenCode 1.18.21, using a scripted provider without answering-model
  inference, passed ordinary startup and observed execution in allowed, denied
  and stale-source cases. Observed initial input was 3,075 bytes in each case,
  with required selected context preserved. Allowed and stale cases had two
  main requests and one tool result; denial had one main request and no tool
  result. Each observed case retained its own process outcome.

These establish interface behavior and source exposure. They do not establish
model-selected use, better answers, task acceptance or durable memory benefit.
Aggregate task budgeting, native compaction and task-level attribution remain
open. The initial input may also reach a harness's auxiliary model requests.

## Failures retained

The first designated-reader test reached the old ownership refusal. The retained
read test initially had no `RunIndex` method; the API test then exposed acceptance
of an unconfigured recipient, and the runner test reached its old body-only guard.
All were checked before implementing their corresponding behavior.

One new evidence test used a non-observing operator fixture and correctly failed
its reader-designation request. It now uses an observing host; the role gate was
preserved. The generated CLI-child source's newline quoting was corrected before
execution, and its Python source was compiled before the integration run.

The first native attempt timed out after an auxiliary request and before a main
request or Cairn tool call. A bounded tool-debug diagnostic reached argument
validation. A traced rerun passed all ordinary startup cases and the first
observed body/outcome check, then exposed a report-variable shadowing error in the
test. After correcting that report, a clean run passed all six cases. The initial
timeout's cause is not established; subsequent success does not erase it.
The isolated fixtures disable OpenCode default plugins; owner configuration is
unchanged.

## Provenance

[Machine-readable evidence](observed-index-2026-09-09.json) retains check paths,
hashes, failures and doctrine identities. Scratch outputs remain outside Git.
The selected decision uses validated Pincite packet `pkt-79ae4d0db9c1674a`,
corpus `corpus-2026-07-12-a11702cc9217` and doctrine
`doctrine-f6bbb5196a3f8bf9`. Its typed decision receipt and consumption records
are under `/tmp/cairn-observed-index-*`. Remaining generic nil-interface and
performance obligations are explicitly nonmaterial to this feature; no new
interface-valued absence contract or performance claim is introduced.


## Local installation

Installed clean `20b7574cfc3015fc9007f97eaa82abbae234fb9b` as CLI, API and ignored
project `bin/cairn` after a dedicated-store backup. SHA-256:
`8884576d7282f404d4269daf60e95aa901206828e976f0125762430a45fb9b55`.
Migration 031 is applied. API PID 1096559 matches the installed binary; PostgreSQL
PID 163669, adapter bytes, credential configuration, Codex/OpenCode settings and
semantic worker source remain unchanged. Version responses match client/server.

An installed retained-index run read the existing OpenCode guide v12 through its
designated ordinary profile: receipt `63a8e554-cc5f-4b01-8559-e8366b51acc1`,
body SHA-256 `694dd8d524e1454b3ff1988a78e92908fe2ff7537dcc691312b7d0876f0f33bb`,
6,707 bytes, with three credits remaining. Process completion is retained as
outcome `f4b1536e-2a36-4f21-ae91-930fc0d61079`. This checks the installed route;
it is not a model task or a task-benefit result.

The same selected procedure, `73537cbd-0ec3-4311-98c9-23e58a685b2f`, is now v13.
Its complete prior body and metadata are preserved; an addition explains the
observed route, differing budget semantics and unchanged ordinary-start contract.
Exact revision retry and a fresh ordinary-profile pull pass. New body SHA-256:
`3ef6529f65fa1cfdc03d786e964f0987b20860db6e515887af459b77a145ee01`.
No raw note body or operational index is committed.

[CI 34439862896](https://github.com/halbritt/cairn/actions/runs/34439862896)
passed for the installed source, including Go/race, 40 Python tests, static/build
checks and authenticated CLI/stdio workflows. Native OpenCode verification is
the separately recorded local scripted-provider run.
