# Design sources

These are exact snapshots of the supplied design and its three named synthesis
and correction documents. See `manifest.json` for original locations and hashes.
Relative links within the source files are preserved; references to Bonded Memory,
Council message numbers, and the original RFP require the original local artifacts.
These documents are design inputs, not operational memory or authority grants.
